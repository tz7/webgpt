{% extends 'base.html.twig' %}



{% block title %}Log in!{% endblock %}

{% block navbar %}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center align-items-center min-vh-100">
            <div class="col-3">
                <div class="card shadow login-card">
                    <div class="card-body mt-3">
                        <form method="post">
                            {% if error %}
                                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}

                            {% if app.user %}
                                <div class="mb-3">
                                    You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                                </div>
                            {% endif %}

                            <h1 class="h3 mb-3 font-weight-normal text-center">Please sign in</h1>
                            <div class="form-group">
                                <label for="inputUsername">Username</label>
                                <input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control" autocomplete="username" required autofocus>
                            </div>
                            <div class="form-group mt-1">
                                <label for="inputPassword">Password</label>
                                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                            </div>

                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                            <button id="sendButton" class="btn btn-lg btn-primary mt-3 w-100" type="submit">Sign in</button>
                        </form>
                        <a href="{{ path('app_register') }}" class="mb-4 mt-1 d-flex justify-content-end">Register</a>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}


{#Old:#}

{#<div class="d-flex justify-content-center align-items-center min-vh-100">#}
{#    <div class="login-container">#}
{#        <form method="post">#}
{#            {% if error %}#}
{#                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>#}
{#            {% endif %}#}

{#            {% if app.user %}#}
{#                <div class="mb-3">#}
{#                    You are logged in as {{ app.user.userIdentifier }}, <a#}
{#                            href="{{ path('app_logout') }}">Logout</a>#}
{#                </div>#}
{#            {% endif %}#}

{#            <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>#}
{#            <div class="form-group">#}
{#                <label for="inputUsername">Username</label>#}
{#                <input type="text" value="{{ last_username }}" name="username" id="inputUsername"#}
{#                       class="form-control"#}
{#                       autocomplete="username" required autofocus>#}
{#            </div>#}
{#            <div class="form-group">#}
{#                <label for="inputPassword">Password</label>#}
{#                <input type="password" name="password" id="inputPassword" class="form-control"#}
{#                       autocomplete="current-password" required>#}
{#            </div>#}

{#            <input type="hidden" name="_csrf_token"#}
{#                   value="{{ csrf_token('authenticate') }}"#}
{#            >#}

{#            #}{##}
{#            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.#}
{#            See https://symfony.com/doc/current/security/remember_me.html#}

{#            <div class="form-group form-check">#}
{#                <input type="checkbox" class="form-check-input" name="_remember_me" id="rememberMe">#}
{#                <label class="form-check-label" for="rememberMe">Remember me</label>#}
{#            </div>#}
{#            #}
{#            <button id="sendButton" class="btn btn-lg btn-primary mt-2" type="submit">Sign in</button>#}
{#            #}{#                <button class="btn btn-lg btn-primary btn-block" type="submit">#}
{#            #}{#                    Sign in#}
{#            #}{#                </button>#}
{#        </form>#}
{#    </div>#}
{#</div>#}
